import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  icon: {
    marginRight: -20,
  },
  header: {
    paddingVertical: 16,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
